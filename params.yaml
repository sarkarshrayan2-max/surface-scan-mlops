train:
  model_name: efficientnetb0
  learning_rate: 0.001
  epochs: 10
  batch_size: 32
  patience: 5
  train_csv: data/interim/splits/train.csv
  validation_csv: data/interim/splits/validation.csv
  test_csv: data/interim/splits/test.csv
  output_dir: artifacts/training
  reports_dir: reports/training
  tracking_uri: sqlite:///mlflow.db
  experiment_name: surface-scan-training
  run_name: efficientnetb0-final-test

evaluate_champion:
  model_path: artifacts/training/efficientnetb0-final-test_best.keras
  split_csv: data/interim/splits/test.csv
  split_name: test
  batch_size: 32
  output_dir: reports/evaluation
